{
  "name": "mocha-supertest",
  "version": "1.0.0",
  "description": "Using supertest in Mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require test/helper.js test",
    "test:watch": "mocha --watch --require test/helper.js test"
  },
  "author": "Kyle Krull",
  "license": "ISC",
  "devDependencies": {
    "expect": "^1.20.2",
    "express": "^4.14.0",
    "mocha": "^3.0.2",
    "supertest": "^2.0.0"
  }
}
